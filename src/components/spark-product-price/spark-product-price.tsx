export interface SparkProductPriceProps {
    parentId: string;
}

/**
 * This component was created by SparkLayer.
 */
export const SparkProductPrice = ({ parentId }: SparkProductPriceProps) => {
    return <spark-product-price parent-id={parentId} />;
};
