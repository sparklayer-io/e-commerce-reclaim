export interface SparkProductRRPProps {
    parentId: string;
}

/**
 * This component was created by SparkLayer.
 */
export const SparkProductRRP = ({ parentId }: SparkProductRRPProps) => {
    return <spark-product-rrp parent-id={parentId} />;
};
