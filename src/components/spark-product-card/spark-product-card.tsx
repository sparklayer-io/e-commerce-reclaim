export interface SparkProductCardProps {
    parentId: string;
}

/**
 * This component was created by SparkLayer.
 */
export const SparkProductCard = ({ parentId }: SparkProductCardProps) => {
    return <spark-product-card parent-id={parentId} />;
};
