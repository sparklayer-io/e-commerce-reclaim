export interface SparkProductMatrixProps {
    parentId: string;
}

/**
 * This component was created by SparkLayer.
 */
export const SparkProductMatrix = ({ parentId }: SparkProductMatrixProps) => {
    return <spark-product-matrix parent-id={parentId} />;
};
