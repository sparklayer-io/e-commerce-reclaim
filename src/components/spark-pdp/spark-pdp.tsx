export interface SparkPDPProps {
    parentId: string;
}

/**
 * This component was created by SparkLayer.
 */
export const SparkPDP = ({ parentId }: SparkPDPProps) => {
    return <spark-pdp parent-id={parentId} />;
};
